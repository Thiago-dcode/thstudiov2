{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": [".opencode/skills"]
  },
  "mcp": {
    "postgres": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-postgres", "postgres://{env:DB_USERNAME}:{env:DB_PASSWORD}@{env:DB_HOST}:{env:DB_PORT}/{env:DB_NAME}"]
    }
  }
}
