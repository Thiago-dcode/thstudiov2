# ---------------------------------------------------------------------------
# Cloudflare origin trust.
#
# Included at http level (before any server block) from conf.d/default.conf.
#
# Two jobs:
#   1. Rewrite $remote_addr to the real visitor IP (CF-Connecting-IP), so logs,
#      rate limiting and anything IP-based see the client rather than Cloudflare.
#   2. Expose $is_cloudflare so the TLS server blocks can refuse direct-to-origin
#      traffic. The origin IP is reachable on the public internet, so without this
#      an attacker can skip Cloudflare's WAF/rate-limiting entirely by connecting
#      to the droplet address.
#
# IMPORTANT: the allowlist is evaluated against $realip_remote_addr, NOT
# $remote_addr. ngx_http_realip_module runs in the POST_READ phase, before the
# access module, so by the time allow/deny or a geo lookup on $remote_addr runs it
# already holds the *visitor's* address — a naive `allow <cf ranges>; deny all;`
# would therefore reject everyone. $realip_remote_addr preserves the address of the
# peer that actually connected, which is what we want to check.
#
# Refresh the ranges from https://www.cloudflare.com/ips-v4 and .../ips-v6.
# ---------------------------------------------------------------------------

set_real_ip_from 173.245.48.0/20;
set_real_ip_from 103.21.244.0/22;
set_real_ip_from 103.22.200.0/22;
set_real_ip_from 103.31.4.0/22;
set_real_ip_from 141.101.64.0/18;
set_real_ip_from 108.162.192.0/18;
set_real_ip_from 190.93.240.0/20;
set_real_ip_from 188.114.96.0/20;
set_real_ip_from 197.234.240.0/22;
set_real_ip_from 198.41.128.0/17;
set_real_ip_from 162.158.0.0/15;
set_real_ip_from 104.16.0.0/13;
set_real_ip_from 104.24.0.0/14;
set_real_ip_from 172.64.0.0/13;
set_real_ip_from 131.0.72.0/22;
set_real_ip_from 2400:cb00::/32;
set_real_ip_from 2606:4700::/32;
set_real_ip_from 2803:f800::/32;
set_real_ip_from 2405:b500::/32;
set_real_ip_from 2405:8100::/32;
set_real_ip_from 2a06:98c0::/29;
set_real_ip_from 2c0f:f248::/32;

# Cloudflare sets CF-Connecting-IP to the true client. Do not use X-Forwarded-For
# here: it is client-supplied and arrives forgeable on direct-to-origin requests.
real_ip_header CF-Connecting-IP;
real_ip_recursive off;

geo $realip_remote_addr $is_cloudflare {
    default         0;

    173.245.48.0/20     1;
    103.21.244.0/22     1;
    103.22.200.0/22     1;
    103.31.4.0/22       1;
    141.101.64.0/18     1;
    108.162.192.0/18    1;
    190.93.240.0/20     1;
    188.114.96.0/20     1;
    197.234.240.0/22    1;
    198.41.128.0/17     1;
    162.158.0.0/15      1;
    104.16.0.0/13       1;
    104.24.0.0/14       1;
    172.64.0.0/13       1;
    131.0.72.0/22       1;
    2400:cb00::/32      1;
    2606:4700::/32      1;
    2803:f800::/32      1;
    2405:b500::/32      1;
    2405:8100::/32      1;
    2a06:98c0::/29      1;
    2c0f:f248::/32      1;

    # Docker bridge + loopback, so container healthchecks and on-box curl work.
    127.0.0.0/8         1;
    172.16.0.0/12       1;
    10.0.0.0/8          1;
}
