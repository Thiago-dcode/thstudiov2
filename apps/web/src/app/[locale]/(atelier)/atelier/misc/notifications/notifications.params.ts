/** Shared between the server page (which reads them) and the filter bar (which writes them). */
export const NOTIFICATIONS_PATH = "/atelier/misc/notifications";
export const DEFAULT_NOTIFICATIONS_PER_PAGE = 15;
