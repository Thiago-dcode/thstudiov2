# Daily Log — 22 Feb 2026

## ✅ Done

- **Username/password reset columns** — added `username_reset_count`, `password_reset_count`, `next_username_reset`, `next_password_reset` to user migration, schema, and types (`packages/database`, `packages/common-lib`). Fixed double-underscore typo in `next__username_reset`.

- **Reset-count enforcement (API)** — `users.service.ts` now properly enforces monthly limits on username and password changes with period-expiry logic: counters auto-reset when `next_*_reset` date passes (`apps/api`).

- **Reset-count enforcement (Web)** — username and password edit dialogs show remaining changes, reset date, and a styled "monthly limit reached" block when exhausted (`apps/web` settings dialogs).

- **Auth guard sync** — added `next_username_reset` and `next_password_reset` to the JWT payload mapping in `auth.guard.ts` so the full `UserAuth` shape is available downstream (`apps/api`).

- **Subscription logic refactor** — moved `useHandleAction`, `paymentMethod` state, and `submitSubscription` from `ChangeSubscriptionDialog` into `PlanSubscriptionProvider`. Dialog is now pure UI reading from context (`apps/web` plan-subscriptions module).

- **`PlanSubscriptionList` component** — extracted reusable plan-list component from `step5.tsx`. Used in both onboarding funnel and settings subscription page (`apps/web` plans module).

- **Settings subscription section** — replaced inline `SubscriptionSection` (with embedded `PlanCard` grid) with a lightweight summary row showing current plan name, billing type, renewal date, and a link to the new subscription page (`apps/web` settings page).

- **Subscription management page** — new `/atelier/settings/subscription` page with full plan picker using `PlanSubscriptionList` (`apps/web`).

- **Subscription callback page** — new `/atelier/settings/subscription/[callback]` for post-payment success/failed flow, adapted from the onboarding funnel callback without any funnel/get-started dependencies (`apps/web`).
