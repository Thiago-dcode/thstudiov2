# Daily Log — 21 Feb 2026

## ✅ Done

- **Password change feature** — full stack: DTO, `POST :id/password` endpoint, service logic, Zod schema, server action, dialog UI, settings page (`/atelier/settings`).

- **Avatar content moderation** — AI service now moderates avatar uploads before saving. Refactored `AiModule` / `AiProcessorModule` to fix circular dependency.

- **`update-user.action` error handling** — switched to field-level `inputErrors` (via `getObjErrorFromZod`) for Zod & file errors; `getFriendlyApiErrors` for API errors; `revalidateTag` on success only.

- **Field-level errors in edit-user UI** — `EditProfile`, `EditAvatar`, `EditBanner` now show per-field errors. `FileInput` component gains an `error` prop.

- **Ban logic fix** — `banUser()` now only called on **permanent** bans. Temporary bans enforced by the guard via `ban_lift` date.
