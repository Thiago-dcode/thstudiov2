# Daily Log — 25 Feb 2026

## ✅ Done

- **Public media page** — `(public)/media/[public_id]/page.tsx` full-screen media view with shape-based sizing (`LANDSCAPE`/`PORTRAIT`/`SQUARE`), title top-left and @username bottom-right overlaid on image container; `max-h-screen` layout (`apps/web`).

- **MediaWithUser API** — `findById` and `findOneByColumn` return `MediaWithUser` (media + user); added `MediaWithUserSchema`, `MediaUser`, `MediaWithUser` types and `formatMediaWithUser` (`apps/api` media module, `packages/common-lib`).

- **MediaPortfolio public_id** — extended type for portfolio media links to `/media/[public_id]` (`packages/common-lib`).

- **Next Image migration** — replaced `<img>` with `next/image` in `PortfolioMediaCard` and public media page; added frontend rule to always use `next/image` (`apps/web`, `.cursor/rules/frontend.mdc`).
