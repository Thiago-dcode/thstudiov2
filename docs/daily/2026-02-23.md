# Daily Log — 23 Feb 2026

## ✅ Done

- **Responsive admin sidebar** — desktop sidebar now collapses/expands via `MainNavProvider` context; mobile gets a slide-out drawer with overlay and auto-close on route change (`apps/web` `admin-header`, `main-nav`, `top-nav`, `main-nav.provider`).

- **User profile backend** — new `getUserProfile` repository method joining `users`, `addresses`, `user_categories`, and `categories` with alias-based collision prevention; `formatUserProfile` aggregates rows into a typed `UserProfile` (`apps/api` users module, `packages/common-lib` schemas/types).

- **User profile API endpoint** — public `GET profile/:username` route returning profile with resolved avatar/banner assets (`apps/api` users controller/service).

- **User profile web service** — added `getProfile(username)` calling the new API endpoint (`apps/web` users service).

- **Public artist profile page** — `[artist]/page.tsx` fetches profile by URL param and renders banner, avatar, name, bio, location, and category badges; shows a "not found" fallback with home link (`apps/web` public route).

- **Top-nav profile link** — added a minimal "View profile" link with external icon in the admin top nav, opening the user's public page in a new tab (`apps/web` `top-nav`, admin layout).

- **Collision prevention guide** — documented the multi-table join aliasing pattern as a `.cursor/rules` MDC rule for future AI reference (`COLLISION-PREVENTION-GUIDE.mdc`).
